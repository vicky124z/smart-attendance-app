SECRET_KEY=change-this-secret-key-in-production
DEBUG=True
ALLOWED_HOSTS=*

DB_NAME=smart_attendance_db
DB_USER=smart_attendance
DB_PASSWORD=smart_attendance_pass
DB_HOST=127.0.0.1
DB_PORT=5432

# Comma separated list of allowed CORS origins for the Flutter web build
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# QR / attendance session code lifetime, in seconds
SESSION_CODE_TTL_SECONDS=1800
